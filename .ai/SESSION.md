# Session — MaterialScope

**Purpose:** **Current working state**, **carryover**, and **next step** only. Not an inventory of the repo, not a durable decision log—use **`DECISIONS.md`** for the latter; **`TASK.md`** for the active slice.

## Carryover

- **Project:** MaterialScope  
- **Direction (context):** incremental Streamlit → Dash + Plotly; DTA parity plan in `c:\Users\Utku ŞAHİN\.cursor\plans\dash_dta_parity_audit_292c7597.plan.md` (audit + phased approach + first safe slice).  
- **Branch:** `web-dash-plotly-migration`; last pushed commit is `d1a154c` (Phase 2a+2b + i18n/palette fix). Phase 3a (stepwise `dbc.Tabs` layout for DTA), Phase 3b (processing preset CRUD + DTA preset panel), and Phase 3c (richer results summary — dataset metadata parity) are **all locally implemented + tested but not yet committed or pushed** — user explicitly deferred commit until all of Phase 3 is done.  
- **WIP / cautions:** Large set of **unrelated modified** tracked files remains locally from prior sessions (see `git status` — backend/core/desktop/packaging/tests/tools/ui/utils drift and untracked `.cursor/rules/00-workflow.mdc`, `pytest_temp/`, `python3`); **do not mix** with the next slice unless intentional. Phase 3 diff (3a + 3b) is isolated to: `dash_app/pages/dta.py`, `dash_app/api_client.py`, `backend/app.py`, `backend/models.py`, `utils/i18n.py`, `tests/test_dta_dash_page.py`, `tests/test_backend_presets_api.py` (new file), `.ai/SESSION.md`, `.ai/TASK.md`.  
- **Last completed (2026-04-16 — Phase 3c DTA parity slice, local only):** richer Results Summary parity with Streamlit's `ui/dta_page.py::_render_dta_results` header. **DTA page (`dash_app/pages/dta.py`):** new `_format_dataset_metadata_value` + `_build_dta_dataset_summary` helpers that render a `html.Dl` definition list with dataset filename, sample name, sample mass (with `mg` unit), and heating rate (with `°C/min` / `°C/dk` unit). Mass and heating-rate rows are omitted when absent; dataset label falls back through `metadata.file_name` → `dataset.display_name` → `result.dataset_key` → `na`. Sample label reuses the existing `_resolve_dta_sample_name` fallback chain. New `result_placeholder_card("dta-result-dataset-summary")` mounted **above** `dta-result-metrics` in the right column. `display_result` callback extended to **6** outputs (summary panel first) and reuses the already-fetched `workspace_dataset_detail` payload — no new backend calls. Empty state and backend-error paths both keep the summary panel with the localized `dash.analysis.dta.summary.empty` message. **i18n (`utils/i18n.py`):** 8 new TR+EN keys — `dash.analysis.dta.summary.{card_title,dataset_label,sample_label,mass_label,heating_rate_label,mass_unit,heating_rate_unit,empty}` (Turkish: Analiz Özeti / Veri Seti / Numune / Kütle / Isıtma Hızı; `°C/dk` for heating rate). **Tests:** +10 tests in `tests/test_dta_dash_page.py` covering layout ordering (`dta-result-dataset-summary` above `dta-result-metrics`), `_format_dataset_metadata_value` edge cases (None/whitespace/string/numeric/NaN), EN full-render with all four fields, TR label + unit translation, mass/heating-rate omission when absent, dataset-key fallback when metadata empty, `display_result` emitting 6 panels on empty state (EN + TR), full success path populating the summary with all four fields via monkeypatched `workspace_result_detail` + `workspace_dataset_detail`, backend-error path keeping the empty summary + surfacing error alert on metrics. **Verify (ran):** `python -m pytest tests/test_dta_dash_page.py -x -q` → **86 passed** in 6.80s (was 76 post-3b, +10 with 3c); adjacent `python -m pytest tests/test_backend_api.py tests/test_backend_details.py tests/test_backend_presets_api.py -x -q` → **49 passed, 1 skipped** in 14.32s. No regressions.
- **Previous slice (2026-04-16 — Phase 3b DTA parity slice, local only):** processing-preset CRUD via four new FastAPI endpoints + matching `api_client` helpers + new DTA preset panel inside the Processing tab. **Backend:** new `PresetSummary` / `PresetListResponse` / `PresetSaveRequest` / `PresetSaveResponse` / `PresetLoadResponse` / `PresetDeleteResponse` models in `backend/models.py`; four new routes in `backend/app.py` next to the figure-register / compare routes — `GET /presets/{analysis_type}`, `POST /presets/{analysis_type}`, `GET /presets/{analysis_type}/{preset_name}`, `DELETE /presets/{analysis_type}/{preset_name}`, all guarded by `_require_token` via `X-TA-Token`, all backed by the existing `core.preset_store` SQLite store so the Dash page now shares its preset persistence with Streamlit. `PresetLimitError` → HTTP 409, `PresetStoreError` (invalid name, unsupported analysis_type, missing `workflow_template_id`) → HTTP 400, missing preset on load/delete → HTTP 404. **api_client (`dash_app/api_client.py`):** new `list_analysis_presets`, `save_analysis_preset`, `load_analysis_preset`, `delete_analysis_preset` helpers following the `register_result_figure` / `literature_compare` style. **DTA page (`dash_app/pages/dta.py`):** new `_preset_controls_card()` helper rendering title + count/max caption + preset dropdown + Apply/Delete buttons + new-name input + Save button + status region, mounted as the **first** `dbc.Tab` child of the Processing tab so preset controls share the same step as smoothing/baseline/peak. New `dcc.Store("dta-preset-refresh", data=0)` added to `_processing_draft_stores`. Six new callbacks: `render_dta_preset_chrome` (locale), `refresh_dta_preset_options` (re-fetches list on refresh-token increment + locale change, formats count/max caption), `toggle_dta_preset_action_buttons` (disables Apply/Delete when no dropdown selection), `apply_dta_preset` (loads preset, pushes current draft onto existing undo stack, replays `smoothing`/`baseline`/`peak_detection` sections onto draft, restores `workflow_template_id` on `dta-template-select.value` when still a valid template id — dash.no_update otherwise), `save_dta_preset` (POSTs draft + current template, clears name input, increments refresh token, surfaces success/error status), `delete_dta_preset` (deletes, clears selection, increments refresh token, surfaces success/error status). All lazy-import `dash_app.api_client` so offline backend just yields a human-readable status message. **i18n (`utils/i18n.py`):** 19 new TR+EN keys under `dash.analysis.dta.presets.*` (title, caption with `{analysis_type}/{count}/{max_count}` placeholders, select_label, select_placeholder, apply_btn, delete_btn, save_name_label, save_name_placeholder, save_btn, applied/saved/deleted success, apply_failed/save_failed/delete_failed/list_failed with `{error}` placeholder, save_name_required, select_required). **Tests:** new `tests/test_backend_presets_api.py` with **10** endpoint tests isolating storage via `monkeypatch.setenv("MATERIALSCOPE_HOME", tmp_path)` (empty list, save+list roundtrip, upsert does not consume a slot, overflow→409, empty-name→400, load unknown→404, load normalized payload, delete then 404-on-second, token required, unsupported analysis_type→400); **+12** tests in `tests/test_dta_dash_page.py` (preset card IDs mounted inside Processing tab, `dta-preset-refresh` store present, 4 api_client helper forwarding tests, apply-pushes-undo+updates-draft+template-value, apply ignores unknown template id (no_update), refresh populates dropdown + caption, refresh surfaces backend error verbatim, chrome TR/EN flip, button-disabled when selection empty). **Verify (ran):** `python -m pytest tests/test_backend_presets_api.py -x -q` → **10 passed** in 3.67s; `python -m pytest tests/test_dta_dash_page.py -x -q` → **69 passed** in 7.22s (was 57 post-3a, +12 with 3b); combined preset suite `python -m pytest tests/test_backend_presets_api.py tests/test_preset_store.py -q` → **14 passed**; adjacent `python -m pytest tests/ -q -k "preset or dash or backend_api or i18n"` → **164 passed, 1 skipped, 657 deselected, 8 warnings** in 21.06s. No regressions.  
- **Previous slice (2026-04-16 — Phase 3a DTA parity slice, local only):** wrapped the DTA left column's 6 cards in a stepwise `dbc.Tabs` (Setup / Processing / Run) via new `_dta_left_column_tabs()` helper. All existing element IDs stay mounted because `dbc.Tab` children render eagerly — no existing callback wiring touched. New `render_dta_tab_chrome` locale callback flips tab labels via three `dash.analysis.dta.tab.{setup,processing,run}` TR+EN bundle entries (Kurulum / İşleme / Çalıştır ↔ Setup / Processing / Run). Tab shells carry `id="dta-tab-{setup,processing,run}-shell"`; outer tabs container is `id="dta-left-tabs"` with `active_tab="dta-tab-setup"`. +4 tests covered tabs structure, card-title mounting, full-layout ID preservation, locale flip. At 3a completion: `pytest tests/test_dta_dash_page.py` → **57 passed**.  
- **Previous slice (2026-04-16 — i18n + palette fixup, committed as `d1a154c`):** Phase 2a + 2b DTA panels were using untranslated English fallbacks (Smoothing/Baseline/Peak/Literature card titles, labels, status, button chrome) because the 30+ `dash.analysis.dta.{smoothing,baseline,peaks,literature,undo_btn,redo_btn,reset_btn}.*` keys referenced by `render_dta_smoothing_chrome` / `render_dta_baseline_chrome` / `render_dta_peak_chrome` / `render_dta_literature_chrome` / `_render_literature_output` / `_smoothing_status_text` / `_baseline_status_text` / `_peak_status_text` / `compare_dta_literature` were absent from `utils/i18n.py` — every `_literature_t(...)` / `_t(...)` call therefore hit its English literal fallback in TR mode. **Fix in `utils/i18n.py`:** added TR+EN bundle entries for all of them (title/method/window/polyorder/sigma/apply + applied status + undo/redo/reset buttons + baseline title/method/lam/p/apply + peaks title/detect_exo/detect_endo/prominence/distance/apply + literature title/ready/empty/max_claims/persist/compare_btn/claims/claims_empty/comparisons/comparisons_empty/citations/citations_empty/missing_result/error{error}/success). Existing `translate_ui(...)` callsites in the DTA page pick these up automatically via `_literature_t` / `_t` (fallback path no longer triggers). **Palette fix in `dash_app/pages/dta.py`:** `dta-reset-btn` was `color="warning"` (Bootstrap mustard yellow — not overridden by `dash_app/assets/style.css`, clashes with the cream/parchment `--ta-accent` palette) → now `color="secondary"` (outline) matching Undo/Redo and the rest of the codebase convention; `dta-literature-compare-btn` was `color="info"` (Bootstrap cyan — also not overridden) → now `color="primary"` matching every other primary-action button (Apply Smoothing / Apply Baseline / Apply Peaks / Save Project / Run). No changes to cards, alerts, or text classes (those already resolve through overridden tokens). **Verify (ran):** `python -m pytest tests/test_dta_dash_page.py -q` → **53 passed**; broader `python -m pytest tests/ -q -k "dash or i18n or dta or literature"` → **236 passed, 560 deselected, 8 warnings**; spot-checked TR/EN rendering via `translate_ui` for 8 representative keys.  
- **Previous slice (2026-04-16 — Phase 2b DTA parity slice, local only):** manual literature-compare panel + figure capture for Report Center. **Backend:** new `ResultFigureRegisterRequest` / `ResultFigureRegisterResponse` in **`backend/models.py`**; new modality-agnostic `POST /workspace/{project_id}/results/{result_id}/figure` endpoint in **`backend/app.py`** that writes base64 PNG into `state["figures"][label]` and de-dupes the label into `state["results"][result_id]["artifacts"]["figure_keys"]` (404 on unknown rid, 400 on bad base64 / empty label, 409 on duplicate label without `replace=True`). **Dash api_client:** new `literature_compare(project_id, result_id, ...)` and `register_result_figure(project_id, result_id, png_bytes, *, label, replace=False)` in **`dash_app/api_client.py`** (base64-encodes bytes). **DTA page (`dash_app/pages/dta.py`):** extracted `_build_dta_go_figure` returning a bare `go.Figure`, `_build_figure` now wraps it in `dcc.Graph` (double-call acceptable; behavior preserved); new `_literature_compare_card()` rendered in the results-summary column; new `dcc.Store("dta-figure-captured", data={})`; new callbacks `render_dta_literature_chrome`, `toggle_literature_compare_button`, `compare_dta_literature` (posts to backend, renders claims + comparisons + citations), `capture_dta_figure` (fires once per saved `result_id`, rebuilds the figure, renders PNG via `plotly.io.to_image(..., engine="kaleido")`, posts via `register_result_figure`; degrades silently if kaleido unavailable). **Tests:** +10 tests in `tests/test_dta_dash_page.py` (layout, api_client forwarding for both helpers, backend register happy path + 409 conflict + replace, unknown rid 404 / empty label 400 / bad base64 400, capture callback dedup behavior, capture callback graceful degrade when `pio.to_image` raises, literature-compare callback render + gating, button-disabled gating). **Verify (ran):** `python -m pytest tests/test_dta_dash_page.py -q --tb=short` → **53 passed** (was 43 in Phase 2a); adjacent `python -m pytest tests/test_backend_api.py tests/test_backend_modality_dispatch.py tests/test_backend_startup.py tests/test_backend_exports.py -q --tb=short` → **45 passed, 2 skipped**.  
- **Earlier slice (2026-04-16 — Phase 2a, local only):** baseline + peak-detection overrides wired into the Phase 1 `processing_overrides` channel. `dash_app/pages/dta.py`: `_default_processing_draft` now carries `smoothing` + `baseline` (`asls`/`linear`/`rubberband`) + `peak_detection` (endo/exo flags, `prominence`, `distance`); new pure helpers `_normalize_baseline_values`, `_normalize_peak_detection_values`, `_baseline_overrides_from_draft`, `_peak_detection_overrides_from_draft`, `_overrides_from_draft` (union); new `_baseline_controls_card()` + `_peak_controls_card()` + chrome/sync/apply/toggle callbacks; `run_dta_analysis` now forwards the union via `_overrides_from_draft`. Shared undo/redo/reset stack untouched. `tests/test_dta_dash_page.py`: +8 tests. **Verify (ran at end of Phase 2a):** `pytest tests/test_dta_dash_page.py` → **43 passed**.  
- **Earlier slice (2026-04-16 — Phase 1, pushed as `040f31d` + docs sync `0993218`):** `processing_overrides` channel end-to-end (backend + api_client + Dash smoothing controls + tests). See prior session notes in git history for details.  

## Next step

1. **Commit (when user asks):** all of Phase 3 (3a + 3b + 3c) is now locally complete — stage + commit the full Phase-3 diff together per user request. Suggested subject: `feat(dash): DTA Phase 3 — stepwise tabs + processing-preset CRUD + richer results summary`. Keep diff scoped to the Phase-3 files only (see Carryover); do **not** fold in unrelated tracked-file drift. Phase 3 touches: `dash_app/pages/dta.py`, `dash_app/api_client.py`, `backend/app.py`, `backend/models.py`, `utils/i18n.py`, `tests/test_dta_dash_page.py`, `tests/test_backend_presets_api.py` (new), `.ai/SESSION.md`, `.ai/TASK.md`, `.ai/DECISIONS.md`.
2. **Phase 4 (later):** quality dashboard, raw-tab metadata, translation polish, keyboard shortcuts. Any further Results Summary work beyond 3c (applied-processing summary expansion, quality metrics / validation warnings block) is deferred to Phase 4 — the Streamlit `_render_dta_results` **header** (dataset metadata) is now fully at parity; downstream body sections already had parity in earlier phases.

**Process defaults:** single thread, **no PM/architect/QA roleplay**, small safe diffs, explicit verification—full detail in **`00-workflow.mdc`**.
