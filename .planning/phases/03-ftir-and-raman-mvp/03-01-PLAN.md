---
phase: 03-ftir-and-raman-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/modalities/registry.py
  - core/modalities/state_keys.py
  - core/modalities/adapters.py
  - core/processing_schema.py
  - core/data_io.py
  - ui/components/column_mapper.py
  - backend/app.py
  - tests/test_modality_registry.py
  - tests/test_data_io.py
  - tests/test_backend_modality_dispatch.py
autonomous: true
requirements:
  - SPC-01
user_setup: []
must_haves:
  truths:
    - "Stable modality contracts include FTIR and RAMAN with deterministic state keys and registry-driven dispatch compatibility."
    - "Import supports FTIR/Raman text and CSV normalization with confidence-driven warning metadata and low-confidence modality confirmation paths."
    - "JCAMP-DX MVP ingestion is bounded to single-spectrum core cases with explicit unsupported handling for out-of-scope variants."
  artifacts:
    - path: core/modalities/registry.py
      provides: "Stable modality registration entries for FTIR and RAMAN"
      exports: ["get_modality", "require_stable_modality", "stable_analysis_types"]
    - path: core/modalities/state_keys.py
      provides: "Deterministic FTIR/RAMAN analysis-state key mapping"
      contains: "ftir_state_|raman_state_"
    - path: core/data_io.py
      provides: "Spectral import normalization and confidence/warning metadata persistence for FTIR/Raman"
      contains: "FTIR|RAMAN|JCAMP"
    - path: tests/test_data_io.py
      provides: "Automated import coverage for FTIR/Raman text/CSV/JCAMP MVP and warning semantics"
      min_lines: 60
  key_links:
    - from: backend/app.py
      to: core/data_io.py
      via: "dataset import endpoint routes FTIR/Raman payloads through normalized import contract"
      pattern: "read_thermal_data|import"
    - from: core/modalities/registry.py
      to: core/modalities/state_keys.py
      via: "registered stable spectral modalities map to deterministic analysis-state keys"
      pattern: "FTIR|RAMAN"
---

<objective>
Establish Phase 3 import and modality foundations before analysis implementation.

Purpose: satisfy SPC-01 with stable FTIR/Raman onboarding through registry/state contracts and normalized MVP import behavior.
Output: FTIR/Raman stable type registration, spectral import normalization with warning/confidence handling, and import/dispatch tests.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ftir-and-raman-mvp/03-CONTEXT.md
@.planning/phases/03-ftir-and-raman-mvp/03-RESEARCH.md
@core/modalities/registry.py
@core/modalities/state_keys.py
@core/data_io.py
@backend/app.py
@ui/components/column_mapper.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add FTIR/RAMAN stable modality contract entries and state-key support</name>
  <files>core/modalities/registry.py, core/modalities/state_keys.py, core/modalities/adapters.py, tests/test_modality_registry.py, tests/test_backend_modality_dispatch.py</files>
  <action>Register FTIR and RAMAN as stable modalities with explicit defaults and deterministic state-key mappings. Ensure stable-analysis helpers and API modality validation paths recognize the new stable types without regressing DSC/TGA/DTA behavior.</action>
  <verify>pytest -q tests/test_modality_registry.py tests/test_backend_modality_dispatch.py -k "ftir or raman or stable_analysis_types"</verify>
  <done>Stable modality lookup and state-key mapping include FTIR/RAMAN and remain backward-compatible for existing modalities.</done>
</task>

<task type="auto">
  <name>Task 2: Extend import normalization for FTIR/Raman text/CSV workflows</name>
  <files>core/data_io.py, tests/test_data_io.py, backend/app.py</files>
  <action>Introduce FTIR/Raman-aware import inference and normalization for delimited text sources. Preserve confidence/warning metadata contract, enforce wavenumber-first defaults with warning-based review, and support low-confidence modality confirmation flow inputs.</action>
  <verify>pytest -q tests/test_data_io.py tests/test_backend_api.py -k "ftir or raman or import_confidence"</verify>
  <done>FTIR/Raman text and CSV files import into normalized structures with traceable warnings/confidence metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Add JCAMP-DX single-spectrum MVP support with explicit scope boundaries</name>
  <files>core/data_io.py, tests/test_data_io.py, ui/components/column_mapper.py</files>
  <action>Implement JCAMP-DX baseline parser path limited to single-spectrum core files and return explicit unsupported messages for advanced variants. Keep column-mapping and import UX coherent for FTIR/Raman modality selection and fallback behavior.</action>
  <verify>pytest -q tests/test_data_io.py -k "jcamp or jdx or ftir or raman"</verify>
  <done>JCAMP single-spectrum imports succeed, unsupported variants are explicit, and mapper/import UX exposes FTIR/Raman choices clearly.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_modality_registry.py tests/test_backend_modality_dispatch.py
- [ ] pytest -q tests/test_data_io.py -k "ftir or raman or jcamp"
- [ ] Backend import and modality validation remain backward-compatible for DSC/TGA/DTA while enabling FTIR/RAMAN onboarding
</verification>

<success_criteria>
- All tasks completed
- FTIR/RAMAN registered as stable modalities with deterministic state keys
- Text/CSV and JCAMP-DX single-spectrum import behavior implemented with confidence/warning semantics
- Automated tests validate import and stable modality dispatch behavior
</success_criteria>

<output>
After completion, create `.planning/phases/03-ftir-and-raman-mvp/03-01-SUMMARY.md`
</output>

