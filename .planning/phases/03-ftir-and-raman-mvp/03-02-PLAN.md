---
phase: 03-ftir-and-raman-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - core/processing_schema.py
  - core/batch_runner.py
  - core/execution_engine.py
  - core/validation.py
  - core/result_serialization.py
  - core/report_generator.py
  - tests/test_processing_schema.py
  - tests/test_batch_runner.py
  - tests/test_validation.py
  - tests/test_result_serialization.py
autonomous: true
requirements:
  - SPC-02
  - SPC-03
user_setup: []
must_haves:
  truths:
    - "FTIR/Raman preprocessing chain is configurable with guided defaults and advanced overrides, with persisted method context per saved run."
    - "FTIR/Raman analysis outputs ranked Top-N matches with normalized score, confidence band, and evidence fields."
    - "Low-confidence/no-match outcomes are represented explicitly as valid cautioned outputs, not forced auto-match or hard failure."
  artifacts:
    - path: core/processing_schema.py
      provides: "FTIR/Raman workflow templates and processing sections for smoothing, baseline, normalization, peak detection, and matching"
      contains: "FTIR|RAMAN"
    - path: core/batch_runner.py
      provides: "Stable FTIR/Raman execution paths that apply preprocessing and similarity analysis contracts"
      contains: "execute_batch_template"
    - path: core/result_serialization.py
      provides: "Stable FTIR/Raman record serialization for summary, rows, processing, validation, and scientific context"
      contains: "analysis_type"
    - path: tests/test_validation.py
      provides: "Rule coverage for spectral pass/warn/fail and no-match caution semantics"
      min_lines: 80
  key_links:
    - from: core/batch_runner.py
      to: core/processing_schema.py
      via: "spectral execution consumes standardized workflow templates and method context"
      pattern: "ensure_processing_payload|workflow_template_id"
    - from: core/result_serialization.py
      to: core/report_generator.py
      via: "serialized spectral confidence/evidence fields drive report-ready sections"
      pattern: "summary|rows|scientific_context"
---

<objective>
Implement stable FTIR/Raman preprocessing and analysis engine behavior after import foundations.

Purpose: satisfy SPC-02 and SPC-03 with configurable preprocessing, ranked similarity outputs, and explicit no-match handling.
Output: spectral processing schema + execution + validation + serialization coverage.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ftir-and-raman-mvp/03-CONTEXT.md
@.planning/phases/03-ftir-and-raman-mvp/03-RESEARCH.md
@.planning/phases/03-ftir-and-raman-mvp/03-VALIDATION.md
@.planning/phases/03-ftir-and-raman-mvp/03-01-SUMMARY.md
@core/processing_schema.py
@core/batch_runner.py
@core/execution_engine.py
@core/validation.py
@core/result_serialization.py
@core/report_generator.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend processing schema/templates for FTIR/Raman guided pipeline</name>
  <files>core/processing_schema.py, tests/test_processing_schema.py</files>
  <action>Add FTIR/Raman template catalogs and standardized processing sections that encode the default order Smooth -> Baseline -> Normalize plus spectral analysis steps. Preserve existing DSC/TGA/DTA schema behavior and aliases.</action>
  <verify>pytest -q tests/test_processing_schema.py -k "ftir or raman or template"</verify>
  <done>FTIR/Raman processing payloads are standardized, template-driven, and backward-compatible with existing modalities.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FTIR/Raman stable execution with ranked similarity output</name>
  <files>core/batch_runner.py, core/execution_engine.py, tests/test_batch_runner.py, tests/test_backend_batch.py</files>
  <action>Add FTIR and RAMAN execution branches that run preprocessing, peak extraction, and similarity matching against reference inputs, returning Top-N candidates with normalized score + confidence band + evidence payloads. Ensure run/batch paths remain registry-driven and stable.</action>
  <verify>pytest -q tests/test_batch_runner.py tests/test_backend_batch.py -k "ftir or raman or similarity"</verify>
  <done>Stable single/batch FTIR/Raman execution succeeds with deterministic ranked outputs and no-match handling.</done>
</task>

<task type="auto">
  <name>Task 3: Add spectral validation + serialization/report readiness semantics</name>
  <files>core/validation.py, core/result_serialization.py, core/report_generator.py, tests/test_validation.py, tests/test_result_serialization.py</files>
  <action>Implement spectral validation rules and serialization contracts so saved FTIR/Raman records include processing context, match confidence/evidence, and explicit caution semantics for low-confidence/no-match outcomes. Ensure report generator can consume fields without breaking thermal branches.</action>
  <verify>pytest -q tests/test_validation.py tests/test_result_serialization.py tests/test_report_generator.py -k "ftir or raman or caution or no_match"</verify>
  <done>Spectral records validate and serialize consistently for downstream compare/export/report usage with caution-safe semantics.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_processing_schema.py tests/test_batch_runner.py -k "ftir or raman"
- [ ] pytest -q tests/test_validation.py tests/test_result_serialization.py
- [ ] Spectral run/batch outputs include ranked matches or explicit no-match, with score/band/evidence persisted in stable records
</verification>

<success_criteria>
- All tasks completed
- FTIR/Raman preprocessing pipeline is template-based and configurable
- Ranked similarity matching with traceable evidence is implemented
- Validation and serialization support stable spectral outputs with no-match caution semantics
</success_criteria>

<output>
After completion, create `.planning/phases/03-ftir-and-raman-mvp/03-02-SUMMARY.md`
</output>

