---
phase: 01-foundation-contracts-and-execution-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/modalities/__init__.py
  - core/modalities/contracts.py
  - core/modalities/adapters.py
  - core/modalities/registry.py
  - core/modalities/state_keys.py
  - tests/test_modality_registry.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-04
user_setup: []
must_haves:
  truths:
    - "Stable modalities are declared in one explicit registry and expose standardized lifecycle hooks for import, preprocess, analyze, serialize, and report context."
    - "DSC and TGA execute through contract adapters that reuse existing batch runner behavior without changing scientific outputs."
    - "State-key resolution is centralized and deterministic for stable analysis types."
  artifacts:
    - path: core/modalities/contracts.py
      provides: "Typed modality contract and lifecycle hook surface"
      contains: "class ModalityAdapter"
    - path: core/modalities/registry.py
      provides: "Static modality registry with stable lookup helpers"
      exports: ["get_modality", "require_stable_modality", "stable_analysis_types"]
    - path: core/modalities/adapters.py
      provides: "DSC/TGA adapter implementations that wrap existing execution path"
      contains: "execute_batch_template"
    - path: tests/test_modality_registry.py
      provides: "Contract and registry automated coverage for ARCH-01"
      min_lines: 40
  key_links:
    - from: core/modalities/registry.py
      to: core/modalities/adapters.py
      via: "registry entries bind analysis types to adapter instances"
      pattern: "DSC|TGA"
    - from: core/modalities/adapters.py
      to: core/batch_runner.py
      via: "adapter run path delegates to execute_batch_template"
      pattern: "execute_batch_template"
    - from: core/modalities/state_keys.py
      to: tests/test_modality_registry.py
      via: "tests verify stable state-key mapping behavior"
      pattern: "state_key|analysis_state_key"
---

<objective>
Establish the foundation contract and registry layer for stable modalities before touching backend routing.

Purpose: Satisfy ARCH-01 first with a concrete, testable modality interface and explicit registration model that Phase 2+ can extend.
Output: New modality contract package, DSC/TGA adapter implementations, centralized stable state-key resolver, and contract tests.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts-and-execution-path/01-CONTEXT.md
@.planning/phases/01-foundation-contracts-and-execution-path/01-RESEARCH.md
@core/batch_runner.py
@core/processing_schema.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define contract and lifecycle hook surface</name>
  <files>core/modalities/contracts.py, core/modalities/__init__.py</files>
  <action>Create the canonical modality contract using Protocol/dataclass typing and include hook methods for import, preprocess, analyze, serialize, and report_context. Keep the contract strict enough for test validation, but avoid introducing runtime plugin loading or new dependencies in Phase 1.</action>
  <verify>pytest -q tests/test_modality_registry.py -k contract</verify>
  <done>Contract module exists, is importable, and tests can assert required hook attributes for stable entries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DSC/TGA adapters and state-key resolver</name>
  <files>core/modalities/adapters.py, core/modalities/state_keys.py</files>
  <action>Implement DSC/TGA adapters that delegate execution to existing core.batch_runner.execute_batch_template, preserving workflow template and validation behavior. Add a dedicated helper that returns analysis state keys (e.g., dsc_state_* and tga_state_*) from analysis_type plus dataset_key so backend code no longer hard-codes key assembly inline.</action>
  <verify>pytest -q tests/test_batch_runner.py tests/test_modality_registry.py -k "adapter or state"</verify>
  <done>Adapters call the existing runner path and state-key mapping is centralized with tests for DSC/TGA and unsupported types.</done>
</task>

<task type="auto">
  <name>Task 3: Add registry lookup helpers and contract-level tests</name>
  <files>core/modalities/registry.py, tests/test_modality_registry.py</files>
  <action>Implement static registry entries for stable DSC/TGA, add get_modality/require_stable_modality/stable_analysis_types helpers, and write tests that assert lookup behavior, stable filtering, and hook-surface consistency. Ensure unsupported analysis_type failures are explicit and deterministic.</action>
  <verify>pytest -q tests/test_modality_registry.py</verify>
  <done>Registry helpers are present, stable entries are explicit, and ARCH-01 gate tests pass.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_modality_registry.py
- [ ] pytest -q tests/test_batch_runner.py
- [ ] No DSC/TGA scientific kernel files changed (`core/dsc_processor.py`, `core/tga_processor.py`)
</verification>

<success_criteria>
- All tasks completed
- Registry/contract package exists and is used as the single source of stable modality metadata
- Adapter path preserves existing execute_batch_template behavior for DSC/TGA
- Contract gate test exists for ARCH-01 and passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts-and-execution-path/01-01-SUMMARY.md`
</output>
