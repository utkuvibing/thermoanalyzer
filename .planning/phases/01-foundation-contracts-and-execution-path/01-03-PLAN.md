---
phase: 01-foundation-contracts-and-execution-path
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - tests/test_dsc_tga_parity.py
  - tests/test_project_io.py
  - tests/test_backend_api.py
  - tests/test_backend_batch.py
  - tests/test_backend_modality_dispatch.py
  - .planning/phases/01-foundation-contracts-and-execution-path/01-VALIDATION.md
autonomous: true
requirements:
  - ARCH-02
  - ARCH-04
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - "DSC/TGA single-run and batch outputs remain baseline-equivalent after registry/dispatch refactor for stable workflows."
    - "Project archive save/load still restores existing dsc_state_* and tga_state_* data without compatibility breaks."
    - "Phase 1 has explicit automated regression gates that fail on execution-path or compatibility drift."
  artifacts:
    - path: tests/test_dsc_tga_parity.py
      provides: "Golden parity checks for DSC/TGA result summaries, rows, execution status, and workflow template identity"
      min_lines: 80
    - path: tests/test_project_io.py
      provides: "Archive roundtrip compatibility assertions for dsc_state_* and tga_state_* after refactor"
      contains: "dsc_state_"
    - path: tests/test_backend_modality_dispatch.py
      provides: "Regression guard for stable dispatch behavior in single and batch API flows"
      contains: "/analysis/run"
    - path: .planning/phases/01-foundation-contracts-and-execution-path/01-VALIDATION.md
      provides: "Updated validation map linking ARCH-01/02/04 to concrete regression commands"
      contains: "ARCH-02"
  key_links:
    - from: tests/test_dsc_tga_parity.py
      to: backend/app.py
      via: "API-level parity assertions for refactored single and batch runs"
      pattern: "/analysis/run|/workspace/.*/batch/run"
    - from: tests/test_project_io.py
      to: core/project_io.py
      via: "roundtrip compatibility checks for persisted analysis states"
      pattern: "dsc_state_|tga_state_"
    - from: .planning/phases/01-foundation-contracts-and-execution-path/01-VALIDATION.md
      to: tests/test_dsc_tga_parity.py
      via: "validation strategy references parity gate command"
      pattern: "test_dsc_tga_parity"
---

<objective>
Lock Phase 1 regression parity and compatibility so the new execution architecture can ship safely.

Purpose: Satisfy ARCH-02 as a release gate by proving behavior parity and state compatibility after Plans 01-02.
Output: Dedicated parity tests, persistence compatibility assertions, and updated validation strategy mapping for ARCH-01/02/04.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts-and-execution-path/01-VALIDATION.md
@.planning/phases/01-foundation-contracts-and-execution-path/01-01-SUMMARY.md
@.planning/phases/01-foundation-contracts-and-execution-path/01-02-SUMMARY.md
@tests/test_backend_api.py
@tests/test_backend_batch.py
@tests/test_project_io.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add DSC/TGA parity suite for refactored execution paths</name>
  <files>tests/test_dsc_tga_parity.py, tests/test_backend_api.py, tests/test_backend_batch.py</files>
  <action>Create a dedicated parity test module that seeds deterministic datasets and compares post-refactor outcomes against baseline expectations for single-run and batch-run behavior (execution_status, result_id shape, workflow_template_id, summary, rows, validation status). Keep assertions strict enough to catch drift but tolerant of volatile IDs/timestamps.</action>
  <verify>pytest -q tests/test_dsc_tga_parity.py</verify>
  <done>Parity tests exist and fail if refactor changes stable DSC/TGA behavior beyond approved non-deterministic fields.</done>
</task>

<task type="auto">
  <name>Task 2: Reinforce archive compatibility for analysis state persistence</name>
  <files>tests/test_project_io.py</files>
  <action>Extend project archive roundtrip tests to assert state restoration for dsc_state_* and tga_state_* after engine/registry integration, including processing metadata and comparison workspace continuity. Ensure compatibility checks remain tied to current .thermozip schema rather than introducing format changes in Phase 1.</action>
  <verify>pytest -q tests/test_project_io.py -k "round_trip or compatibility"</verify>
  <done>Archive roundtrip tests cover restored analysis states and guard against backward-incompatible persistence changes.</done>
</task>

<task type="auto">
  <name>Task 3: Update regression gate map and dispatch coverage</name>
  <files>tests/test_backend_modality_dispatch.py, .planning/phases/01-foundation-contracts-and-execution-path/01-VALIDATION.md</files>
  <action>Complete the validation map by linking ARCH-01/02/04 to concrete, runnable commands and ensure backend modality dispatch tests are included in focused gate commands. Keep the gate fast enough for per-task runs and explicit enough for release confidence.</action>
  <verify>pytest -q tests/test_backend_modality_dispatch.py tests/test_dsc_tga_parity.py tests/test_project_io.py</verify>
  <done>Validation artifact and test suite together provide explicit automated release gates for regression parity and generic dispatch correctness.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_dsc_tga_parity.py tests/test_backend_modality_dispatch.py tests/test_project_io.py
- [ ] pytest -q tests/test_backend_api.py tests/test_backend_batch.py tests/test_batch_runner.py tests/test_processing_schema.py
- [ ] 01-VALIDATION.md task map reflects implemented tests and commands for ARCH-01, ARCH-02, ARCH-04
</verification>

<success_criteria>
- All tasks completed
- Regression parity gate exists and passes for DSC/TGA single and batch flows
- Archive compatibility remains intact for existing analysis state keys
- Phase validation strategy is aligned with implemented regression gates
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts-and-execution-path/01-03-SUMMARY.md`
</output>
