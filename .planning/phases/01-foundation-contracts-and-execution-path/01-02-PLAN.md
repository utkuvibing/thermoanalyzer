---
phase: 01-foundation-contracts-and-execution-path
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - core/execution_engine.py
  - backend/app.py
  - backend/detail.py
  - backend/models.py
  - tests/test_backend_api.py
  - tests/test_backend_batch.py
  - tests/test_backend_details.py
  - tests/test_backend_modality_dispatch.py
autonomous: true
requirements:
  - ARCH-04
  - ARCH-02
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - "Backend single-run and batch endpoints resolve stable analysis types through registry-backed generic dispatch instead of DSC/TGA-only branching."
    - "Unsupported or unstable analysis types fail with explicit validation errors, not implicit fallthrough."
    - "Compare workspace analysis type validation uses the stable registry set and remains backward compatible for existing DSC/TGA projects."
  artifacts:
    - path: core/execution_engine.py
      provides: "Generic execution service that dispatches stable analysis_type through registry adapters"
      exports: ["run_single_analysis", "run_batch_analysis"]
    - path: backend/app.py
      provides: "Run and batch endpoints migrated to execution engine while preserving response schema"
      contains: "run_single_analysis"
    - path: backend/detail.py
      provides: "Compare workspace update path validates analysis type via stable registry"
      contains: "stable_analysis_types"
    - path: tests/test_backend_modality_dispatch.py
      provides: "API dispatch coverage for generic stable modality handling and explicit error paths"
      min_lines: 50
  key_links:
    - from: backend/app.py
      to: core/execution_engine.py
      via: "analysis/run and workspace batch run delegate orchestration to engine"
      pattern: "run_single_analysis|run_batch_analysis"
    - from: core/execution_engine.py
      to: core/modalities/registry.py
      via: "stable modality lookup and validation"
      pattern: "require_stable_modality|stable_analysis_types"
    - from: backend/detail.py
      to: core/modalities/registry.py
      via: "compare workspace analysis_type validation"
      pattern: "stable_analysis_types"
---

<objective>
Migrate backend execution paths to registry-driven generic dispatch while preserving API compatibility.

Purpose: Deliver ARCH-04 without regressing existing DSC/TGA behavior, using the contract layer from Plan 01.
Output: A reusable execution engine plus endpoint refactors for single run, batch run, and compare workspace analysis_type validation.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts-and-execution-path/01-CONTEXT.md
@.planning/phases/01-foundation-contracts-and-execution-path/01-RESEARCH.md
@.planning/phases/01-foundation-contracts-and-execution-path/01-01-SUMMARY.md
@backend/app.py
@backend/detail.py
@backend/models.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Introduce generic execution engine for stable modalities</name>
  <files>core/execution_engine.py</files>
  <action>Create a backend-facing execution engine that accepts analysis_type, dataset, workflow template id, and state context, then dispatches through registry adapters from Plan 01. Keep row normalization and outcome summarization behavior compatible with current API outputs, and centralize unsupported-modality error generation.</action>
  <verify>pytest -q tests/test_backend_modality_dispatch.py -k engine</verify>
  <done>Execution engine exposes reusable run_single_analysis and run_batch_analysis entry points with deterministic error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor /analysis/run and /workspace/{project_id}/batch/run to use engine dispatch</name>
  <files>backend/app.py, backend/models.py</files>
  <action>Replace hard-coded DSC/TGA endpoint branching with engine calls and registry-based defaults. Preserve existing response models and payload fields so desktop clients are not broken. Continue to persist state via existing project keys and maintain history event semantics.</action>
  <verify>pytest -q tests/test_backend_api.py tests/test_backend_batch.py tests/test_backend_modality_dispatch.py -k "analysis_run or batch_run"</verify>
  <done>Endpoints no longer gate on fixed DSC/TGA literals in control flow, yet all existing endpoint tests remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate compare workspace analysis_type validation to stable registry set</name>
  <files>backend/detail.py, tests/test_backend_details.py, tests/test_backend_modality_dispatch.py</files>
  <action>Update compare workspace write validation to accept registry stable modalities (instead of static {"DSC","TGA"}), while keeping normalized defaults and existing compare payload shape. Add tests for valid stable updates and explicit rejection of non-stable or unknown analysis types.</action>
  <verify>pytest -q tests/test_backend_details.py tests/test_backend_modality_dispatch.py -k compare</verify>
  <done>Compare workspace validation is registry-driven and backward-compatible, with explicit test coverage for acceptance and rejection paths.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_backend_api.py tests/test_backend_batch.py tests/test_backend_details.py tests/test_backend_modality_dispatch.py
- [ ] `backend/app.py` no longer contains endpoint control-flow branches that directly hard-code only DSC/TGA acceptance
- [ ] API response schema compatibility maintained for existing desktop callers
</verification>

<success_criteria>
- All tasks completed
- Stable analysis_type dispatch is centralized and registry-backed
- Single-run and batch endpoints satisfy ARCH-04 through generic handling
- Explicit unsupported analysis_type errors are covered by tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts-and-execution-path/01-02-SUMMARY.md`
</output>
