---
phase: 04-xrd-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/modalities/registry.py
  - core/modalities/state_keys.py
  - core/modalities/adapters.py
  - core/data_io.py
  - ui/components/column_mapper.py
  - backend/app.py
  - tests/test_modality_registry.py
  - tests/test_data_io.py
  - tests/test_backend_modality_dispatch.py
autonomous: true
requirements:
  - XRD-01
user_setup: []
must_haves:
  truths:
    - "Stable modality contracts include XRD with deterministic state key mapping and registry-driven backend acceptance."
    - "XRD import supports `.xy` and `.dat` measured-pattern paths plus bounded `.cif` MVP handling with explicit unsupported messaging."
    - "Normalized XRD datasets persist axis-role, unit, and wavelength provenance needed by downstream processing and reporting."
  artifacts:
    - path: core/modalities/registry.py
      provides: "Stable modality registration entries that include XRD in lookup and stable-analysis helpers"
      exports: ["get_modality", "require_stable_modality", "stable_analysis_types"]
    - path: core/modalities/state_keys.py
      provides: "Deterministic XRD analysis state-key mapping for persisted run context"
      contains: "xrd_state_"
    - path: core/data_io.py
      provides: "XRD import normalization for `.xy/.dat/.cif` MVP with bounded parser policy and explicit metadata contract"
      contains: "XRD|cif|xy|dat"
    - path: tests/test_data_io.py
      provides: "Automated coverage for XRD import normalization and CIF boundary/error semantics"
      min_lines: 70
  key_links:
    - from: backend/app.py
      to: core/data_io.py
      via: "dataset import endpoint routes XRD file inputs through normalized import contract"
      pattern: "read_thermal_data|import"
    - from: core/modalities/registry.py
      to: core/modalities/state_keys.py
      via: "stable XRD registration resolves deterministic persisted state key"
      pattern: "XRD|xrd_state_"
---

<objective>
Establish XRD modality and import foundations so Phase 4 execution can proceed on stable contracts.

Purpose: satisfy XRD-01 with stable XRD onboarding in registry/dispatch and normalized `.xy/.dat/.cif` MVP import behavior.
Output: registry/state-key/adapter updates, bounded XRD import normalization, and dispatch/import regression tests.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-xrd-mvp/04-RESEARCH.md
@.planning/phases/04-xrd-mvp/04-VALIDATION.md
@core/modalities/registry.py
@core/modalities/state_keys.py
@core/modalities/adapters.py
@core/data_io.py
@ui/components/column_mapper.py
@backend/app.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add XRD stable modality contracts and deterministic state keys</name>
  <files>core/modalities/registry.py, core/modalities/state_keys.py, core/modalities/adapters.py, tests/test_modality_registry.py, tests/test_backend_modality_dispatch.py</files>
  <action>Register XRD as a stable modality with explicit dataset eligibility and deterministic `xrd_state_&lt;dataset_key&gt;` mapping. Keep existing DSC/TGA/DTA/FTIR/RAMAN behavior unchanged and preserve sorted stable-analysis helper semantics.</action>
  <verify><automated>pytest -q tests/test_modality_registry.py tests/test_backend_modality_dispatch.py -k "xrd or stable_analysis_types"</automated></verify>
  <done>Registry, adapter, and state-key helpers treat XRD as stable and backend modality validation accepts XRD in stable paths.</done>
</task>

<task type="auto">
  <name>Task 2: Implement normalized `.xy/.dat/.cif` XRD import with bounded CIF scope</name>
  <files>core/data_io.py, tests/test_data_io.py, ui/components/column_mapper.py</files>
  <action>Add XRD import inference and normalization for `.xy` and `.dat` measured-pattern files, plus bounded `.cif` MVP support for allowed structures. Persist axis/wavelength provenance (`xrd_axis_role`, `xrd_axis_unit`, `xrd_wavelength_angstrom`) and return explicit unsupported errors for out-of-scope CIF variants.</action>
  <verify><automated>pytest -q tests/test_data_io.py -k "xrd or cif or xy or dat"</automated></verify>
  <done>XRD imports are normalized and reproducible, with clear supported-scope and unsupported-scope behavior for CIF input.</done>
</task>

<task type="auto">
  <name>Task 3: Wire backend import/dispatch surfaces for XRD onboarding</name>
  <files>backend/app.py, ui/components/column_mapper.py, tests/test_backend_modality_dispatch.py</files>
  <action>Ensure backend import and modality-validation paths route XRD through registry-driven stable handling, and update mapper/selectors to expose XRD choices coherently for import workflows. Preserve existing stable API contracts and errors for unsupported non-stable analyses.</action>
  <verify><automated>pytest -q tests/test_backend_modality_dispatch.py tests/test_backend_api.py -k "xrd or import or analysis_type"</automated></verify>
  <done>API import and validation surfaces consistently recognize stable XRD without regressing current modalities.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_modality_registry.py tests/test_data_io.py tests/test_backend_modality_dispatch.py
- [ ] pytest -q tests/test_backend_api.py -k "xrd or analysis_type or import"
- [ ] XRD `.xy/.dat/.cif` import path enforces bounded CIF policy with explicit unsupported messaging
</verification>

<success_criteria>
- All tasks completed
- XRD is registered as a stable modality with deterministic state keys
- `.xy/.dat/.cif` MVP import normalization is implemented with provenance fields and explicit unsupported boundaries
- Backend stable modality dispatch and import behavior include XRD with automated coverage
</success_criteria>

<output>
After completion, create `.planning/phases/04-xrd-mvp/04-01-SUMMARY.md`
</output>
