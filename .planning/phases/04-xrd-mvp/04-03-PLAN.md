---
phase: 04-xrd-mvp
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - core/batch_runner.py
  - core/validation.py
  - core/result_serialization.py
  - tests/test_batch_runner.py
  - tests/test_validation.py
  - tests/test_result_serialization.py
autonomous: true
requirements:
  - XRD-03
user_setup: []
must_haves:
  truths:
    - "XRD analysis outputs qualitative phase candidates ranked by deterministic evidence metrics, not forced definitive identification."
    - "Each candidate includes traceable evidence fields (shared peaks, overlap score, delta position, unmatched major peaks) and confidence band metadata."
    - "`no_match` and low-confidence outcomes remain valid cautionary stable outputs when schema and provenance requirements are satisfied."
  artifacts:
    - path: core/batch_runner.py
      provides: "Deterministic XRD qualitative matching engine and candidate ranking output contract"
      contains: "confidence|candidate|xrd"
    - path: core/validation.py
      provides: "XRD analysis-output validation rules for candidate evidence completeness and confidence semantics"
      contains: "XRD-C03|XRD-C04|XRD-W03"
    - path: core/result_serialization.py
      provides: "Stable XRD serialization shape for candidate tables, summary confidence fields, and scientific context"
      contains: "analysis_type|summary|rows"
    - path: tests/test_result_serialization.py
      provides: "Automated XRD candidate-evidence and caution-schema coverage"
      min_lines: 80
  key_links:
    - from: core/batch_runner.py
      to: core/result_serialization.py
      via: "qualitative matching output feeds stable serialization summary/rows contract"
      pattern: "top_phase|confidence_band|rows"
    - from: core/validation.py
      to: core/result_serialization.py
      via: "validated match-status and caution semantics propagate into persisted stable records"
      pattern: "validation_status|caution|no_match"
---

<objective>
Deliver qualitative XRD phase-candidate matching with traceable confidence and caution-safe semantics.

Purpose: satisfy XRD-03 by implementing deterministic candidate matching, evidence-rich outputs, and validation/serialization compatibility.
Output: qualitative matching engine behavior in execution path plus validation and serialization contracts/tests.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-xrd-mvp/04-RESEARCH.md
@.planning/phases/04-xrd-mvp/04-VALIDATION.md
@.planning/phases/04-xrd-mvp/04-01-SUMMARY.md
@.planning/phases/04-xrd-mvp/04-02-SUMMARY.md
@core/batch_runner.py
@core/validation.py
@core/result_serialization.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement deterministic XRD qualitative candidate matching</name>
  <files>core/batch_runner.py, tests/test_batch_runner.py</files>
  <action>Add qualitative phase matching from observed peaks to reference candidates using explicit tolerance and deterministic ranking logic. Persist candidate-level evidence fields and confidence labels (`high/medium/low/no_match`) without claiming quantitative phase fractions.</action>
  <verify><automated>pytest -q tests/test_batch_runner.py -k "xrd and (match or candidate or confidence)"</automated></verify>
  <done>XRD execution returns ranked candidate matches with reproducible evidence metrics and explicit confidence/caution semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce XRD match-output validation semantics</name>
  <files>core/validation.py, tests/test_validation.py</files>
  <action>Add XRD analysis-output checks for required evidence payloads, confidence labels, and no-match caution rules. Fail structurally invalid match outputs and warn on low-confidence/no-match scenarios while keeping valid cautionary records saveable.</action>
  <verify><automated>pytest -q tests/test_validation.py -k "xrd and (match or confidence or no_match)"</automated></verify>
  <done>XRD validation distinguishes invalid evidence schema failures from valid cautionary no-match/low-confidence outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Serialize XRD candidate outputs for downstream report/export readiness</name>
  <files>core/result_serialization.py, tests/test_result_serialization.py</files>
  <action>Extend stable result serialization for XRD summaries and candidate rows so downstream report/export paths receive top-phase, score, confidence band, and evidence-rich candidate payloads consistently.</action>
  <verify><automated>pytest -q tests/test_result_serialization.py -k "xrd or candidate or confidence_band"</automated></verify>
  <done>Serialized XRD records include complete candidate-evidence outputs and caution metadata for stable artifact consumption.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_batch_runner.py tests/test_validation.py tests/test_result_serialization.py -k "xrd"
- [ ] XRD no-match and low-confidence outputs remain valid cautionary records when schema/provenance are complete
- [ ] Candidate ranking output includes required traceability fields and deterministic top-N ordering
</verification>

<success_criteria>
- All tasks completed
- XRD qualitative phase matching returns ranked candidates with traceable confidence evidence
- Validation and serialization preserve no-match/low-confidence caution semantics
- Stable XRD records are structurally ready for report/export integration
</success_criteria>

<output>
After completion, create `.planning/phases/04-xrd-mvp/04-03-SUMMARY.md`
</output>
