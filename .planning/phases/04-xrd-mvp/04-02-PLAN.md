---
phase: 04-xrd-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - core/processing_schema.py
  - core/batch_runner.py
  - core/execution_engine.py
  - core/validation.py
  - tests/test_processing_schema.py
  - tests/test_batch_runner.py
  - tests/test_validation.py
autonomous: true
requirements:
  - XRD-02
user_setup: []
must_haves:
  truths:
    - "XRD processing is template-driven with reproducible defaults for axis normalization, smoothing, baseline correction, and peak extraction."
    - "Stable run and batch execution include robust XRD peak detection controls (prominence, distance, width) with deterministic outputs."
    - "Validation gates require processing and peak-detection context before stable artifact eligibility."
  artifacts:
    - path: core/processing_schema.py
      provides: "XRD workflow templates and processing sections required by stable execution payload contracts"
      contains: "xrd|workflow_template_id"
    - path: core/batch_runner.py
      provides: "XRD preprocessing and peak extraction execution path integrated into shared run/batch engine"
      contains: "execute_batch_template|XRD"
    - path: core/validation.py
      provides: "XRD processing-context checks for stable pass/warn/fail decisions"
      contains: "XRD"
    - path: tests/test_batch_runner.py
      provides: "Deterministic XRD preprocessing and robust peak detection test coverage"
      min_lines: 90
  key_links:
    - from: core/batch_runner.py
      to: core/processing_schema.py
      via: "XRD execution consumes standardized processing templates and persists method context"
      pattern: "ensure_processing_payload|workflow_template_id"
    - from: core/execution_engine.py
      to: core/batch_runner.py
      via: "stable run and batch flows delegate XRD execution through shared batch template contract"
      pattern: "execute_batch_template|analysis_type"
---

<objective>
Implement XRD preprocessing and robust peak extraction after import/registry foundations are established.

Purpose: satisfy XRD-02 with template-driven XRD preprocessing plus deterministic peak detection and validation context requirements.
Output: processing schema extensions, batch/execution engine XRD path, and processing/validation tests.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-xrd-mvp/04-RESEARCH.md
@.planning/phases/04-xrd-mvp/04-VALIDATION.md
@.planning/phases/04-xrd-mvp/04-01-SUMMARY.md
@core/processing_schema.py
@core/batch_runner.py
@core/execution_engine.py
@core/validation.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add XRD processing templates and payload sections</name>
  <files>core/processing_schema.py, tests/test_processing_schema.py</files>
  <action>Define XRD workflow templates with explicit defaults for axis normalization, smoothing, baseline/background correction, and peak extraction. Keep existing modality templates backward-compatible and preserve schema helper behavior.</action>
  <verify><automated>pytest -q tests/test_processing_schema.py -k "xrd or template or processing"</automated></verify>
  <done>XRD processing payloads are standardized and template-driven with deterministic defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Implement robust XRD preprocessing and peak detection in execution path</name>
  <files>core/batch_runner.py, core/execution_engine.py, tests/test_batch_runner.py</files>
  <action>Add XRD execution flow that performs ordered preprocessing and robust peak detection using configurable prominence/width/distance controls and deterministic ranking constraints. Ensure output shape remains compatible with current run/batch summary contracts.</action>
  <verify><automated>pytest -q tests/test_batch_runner.py tests/test_backend_batch.py -k "xrd or peak or preprocessing"</automated></verify>
  <done>XRD run and batch execution produce reproducible preprocessed traces and robust peak sets with explicit method context.</done>
</task>

<task type="auto">
  <name>Task 3: Add XRD processing-context validation guards</name>
  <files>core/validation.py, tests/test_validation.py</files>
  <action>Extend validation rules to require XRD processing and peak-detection context for stable reporting eligibility. Differentiate hard failures from caution warnings without forcing false-positive matches.</action>
  <verify><automated>pytest -q tests/test_validation.py -k "xrd or processing_context or peak"</automated></verify>
  <done>Validation enforces XRD processing context requirements and returns deterministic pass/warn/fail semantics.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_processing_schema.py tests/test_batch_runner.py tests/test_validation.py -k "xrd"
- [ ] pytest -q tests/test_backend_batch.py -k "xrd"
- [ ] XRD execution records workflow template id and peak-detection parameters in persisted processing context
</verification>

<success_criteria>
- All tasks completed
- XRD preprocessing pipeline is template-driven and reproducible
- Robust peak detection is implemented with deterministic controls
- Validation gates require XRD processing context for stable artifact flow
</success_criteria>

<output>
After completion, create `.planning/phases/04-xrd-mvp/04-02-SUMMARY.md`
</output>
