---
phase: 02-dta-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/modalities/registry.py
  - core/modalities/adapters.py
  - core/modalities/state_keys.py
  - core/batch_runner.py
  - core/execution_engine.py
  - backend/app.py
  - tests/test_modality_registry.py
  - tests/test_batch_runner.py
  - tests/test_backend_modality_dispatch.py
autonomous: true
requirements:
  - DTA-02
user_setup: []
must_haves:
  truths:
    - "DTA is accepted as a stable analysis type by backend single-run and batch-run validation paths through registry-based dispatch."
    - "Stable DTA dataset eligibility allows DTA and UNKNOWN datasets, with deterministic state key mapping dta_state_<dataset_key>."
    - "DTA execution uses stable workflow template defaults (dta.general) while honoring explicit template overrides including dta.thermal_events."
  artifacts:
    - path: core/modalities/registry.py
      provides: "Stable modality registration that includes DTA in registry-backed lookup helpers"
      exports: ["get_modality", "require_stable_modality", "stable_analysis_types"]
    - path: core/modalities/state_keys.py
      provides: "Deterministic stable key resolver for DTA analysis state persistence"
      contains: "dta_state_"
    - path: core/batch_runner.py
      provides: "DTA batch execution path integrated with existing summary/result contracts"
      contains: "DTAProcessor"
    - path: tests/test_backend_modality_dispatch.py
      provides: "Backend dispatch coverage proving DTA is accepted in stable API paths"
      min_lines: 50
  key_links:
    - from: core/execution_engine.py
      to: core/modalities/registry.py
      via: "stable run and batch orchestration resolve DTA through registry validation"
      pattern: "require_stable_modality|stable_analysis_types"
    - from: core/modalities/adapters.py
      to: core/batch_runner.py
      via: "DTA adapter delegates runtime execution to shared batch runner contract"
      pattern: "execute_batch_template|DTA"
---

<objective>
Stabilize DTA in backend modality contracts and execution routing before UI/report promotion.

Purpose: satisfy DTA-02 by making DTA a true stable analysis type in registry and run/batch execution paths.
Output: registry/state-key/adapter updates, DTA-capable backend execution wiring, and dispatch tests that prove stable DTA acceptance.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dta-stabilization/02-CONTEXT.md
@.planning/phases/02-dta-stabilization/02-RESEARCH.md
@core/modalities/registry.py
@core/modalities/adapters.py
@core/modalities/state_keys.py
@core/execution_engine.py
@core/batch_runner.py
@backend/app.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend stable modality registry contract to include DTA</name>
  <files>core/modalities/registry.py, core/modalities/adapters.py, core/modalities/state_keys.py, tests/test_modality_registry.py</files>
  <action>Add DTA to stable modality registration and adapter wiring, enforce DTA stable dataset eligibility as {"DTA","UNKNOWN"}, and add deterministic analysis key mapping dta_state_&lt;dataset_key&gt;. Keep existing DSC/TGA entries unchanged and preserve registry helper behavior.</action>
  <verify>pytest -q tests/test_modality_registry.py tests/test_backend_modality_dispatch.py -k "dta or stable"</verify>
  <done>Registry and state-key helpers accept stable DTA with explicit eligibility and deterministic key semantics covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DTA stable run/batch execution path</name>
  <files>core/batch_runner.py, core/execution_engine.py, tests/test_batch_runner.py, tests/test_backend_modality_dispatch.py</files>
  <action>Add DTA execution support to batch runner and execution engine dispatch so stable single-run and batch-run flows execute DTA with default template dta.general and explicit override support for dta.thermal_events. Keep result shape compatible with existing API summary rows and status fields.</action>
  <verify>pytest -q tests/test_batch_runner.py tests/test_backend_batch.py tests/test_backend_modality_dispatch.py -k "dta or batch"</verify>
  <done>DTA executes successfully in stable backend paths and unsupported-analysis errors remain explicit for non-stable types.</done>
</task>

<task type="auto">
  <name>Task 3: Wire backend run/batch API validation to stable DTA set</name>
  <files>backend/app.py, tests/test_backend_modality_dispatch.py</files>
  <action>Refactor remaining DSC/TGA-only stable checks in run/batch API validation to use stable modality helpers so DTA is accepted consistently. Preserve request/response schemas and backward compatibility for existing DSC/TGA projects.</action>
  <verify>pytest -q tests/test_backend_api.py tests/test_backend_modality_dispatch.py -k "analysis_type or batch or dta"</verify>
  <done>API stable analysis validation is registry-driven and includes DTA in run and batch behavior with regression coverage.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_modality_registry.py tests/test_batch_runner.py tests/test_backend_modality_dispatch.py
- [ ] pytest -q tests/test_backend_api.py tests/test_backend_batch.py -k "analysis or batch or dta"
- [ ] Stable analysis-type checks in run/batch backend paths are registry-backed rather than hard-coded DSC/TGA-only gates
</verification>

<success_criteria>
- All tasks completed
- DTA accepted in stable run/batch backend contracts
- Deterministic DTA state-key mapping is implemented and tested
- Backend dispatch remains backward-compatible for DSC/TGA while adding DTA
</success_criteria>

<output>
After completion, create `.planning/phases/02-dta-stabilization/02-01-SUMMARY.md`
</output>
