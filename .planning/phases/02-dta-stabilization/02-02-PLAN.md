---
phase: 02-dta-stabilization
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - core/validation.py
  - core/result_serialization.py
  - core/report_generator.py
  - backend/exports.py
  - tests/test_validation.py
  - tests/test_result_serialization.py
  - tests/test_report_generator.py
  - tests/test_export_report.py
  - tests/test_backend_exports.py
autonomous: true
requirements:
  - DTA-03
  - DTA-04
user_setup: []
must_haves:
  truths:
    - "DTA has productionized pass/warn/fail validation with explicit critical blockers and method-context-aware checks."
    - "Stable DTA serialization/report semantics classify Phase 2 DTA outputs as stable rather than experimental."
    - "Export and report generation include modality-appropriate DTA summaries without experimental-only DTA disclaimers in stable flow."
  artifacts:
    - path: core/validation.py
      provides: "DTA-specific validation rule branch with pass/warn/fail and method-context checks"
      contains: "DTA"
    - path: core/result_serialization.py
      provides: "Stable DTA result serialization contract for report/export partitioning"
      contains: "status"
    - path: core/report_generator.py
      provides: "DTA modality-appropriate report summaries for stable records"
      contains: "DTA"
    - path: tests/test_validation.py
      provides: "Automated coverage for DTA rule semantics and blocker/warning policy"
      min_lines: 80
  key_links:
    - from: core/result_serialization.py
      to: core/report_generator.py
      via: "serialized record status drives stable vs experimental report partition behavior"
      pattern: "stable|experimental"
    - from: core/validation.py
      to: core/result_serialization.py
      via: "validation result is reflected in persisted DTA stable output semantics"
      pattern: "validation_status|status"
    - from: backend/exports.py
      to: core/report_generator.py
      via: "export/report paths consume stable DTA records through shared report pipeline"
      pattern: "prepare_export|generate_report"
---

<objective>
Productionize DTA validation and stable report/export semantics after backend dispatch is enabled.

Purpose: satisfy DTA-03 and DTA-04 with deterministic rule behavior, stable-status serialization, and modality-appropriate reporting.
Output: DTA rule catalog in validator, stable DTA serialization/report outputs, and automated test coverage across validation/export/report modules.
</objective>

<execution_context>
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Utku ŞAHİN/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dta-stabilization/02-CONTEXT.md
@.planning/phases/02-dta-stabilization/02-RESEARCH.md
@.planning/phases/02-dta-stabilization/02-VALIDATION.md
@.planning/phases/02-dta-stabilization/02-01-SUMMARY.md
@core/validation.py
@core/result_serialization.py
@core/report_generator.py
@backend/exports.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement DTA production validation branch with pass/warn/fail semantics</name>
  <files>core/validation.py, tests/test_validation.py</files>
  <action>Add DTA-specific validation rules that separate blocking integrity/context failures from non-blocking quality warnings, and include method-context checks aligned with workflow template and sign-convention expectations. Preserve existing validation payload shape (status, issues, warnings, checks) for downstream compatibility.</action>
  <verify>pytest -q tests/test_validation.py -k "dta or pass or warn or fail"</verify>
  <done>DTA validation deterministically emits pass/warn/fail outcomes and only critical failures block stable path eligibility.</done>
</task>

<task type="auto">
  <name>Task 2: Reclassify stable DTA serialization and scientific context language</name>
  <files>core/result_serialization.py, tests/test_result_serialization.py</files>
  <action>Update DTA result serialization so Phase 2 stable DTA records are emitted with stable status and scientifically appropriate modality context wording. Remove experimental-only DTA labeling for stable flow while keeping experimental pathways intact for non-stable records.</action>
  <verify>pytest -q tests/test_result_serialization.py -k "dta and (stable or status or context)"</verify>
  <done>Stable DTA records serialize with correct status and no experimental DTA disclaimer leakage in stable output path.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure report/export include stable DTA summaries end to end</name>
  <files>core/report_generator.py, backend/exports.py, tests/test_report_generator.py, tests/test_export_report.py, tests/test_backend_exports.py</files>
  <action>Adjust report and export handling so stable DTA records are rendered in stable sections with modality-appropriate summary content and validation visibility. Keep CSV/XLSX/report pipeline shape compatible with existing consumers and avoid DSC/TGA regressions.</action>
  <verify>pytest -q tests/test_report_generator.py tests/test_export_report.py tests/test_backend_exports.py -k "dta or stable or export"</verify>
  <done>Reports and exports contain stable DTA summaries in the correct status partition with automated coverage.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q tests/test_validation.py tests/test_result_serialization.py
- [ ] pytest -q tests/test_report_generator.py tests/test_export_report.py tests/test_backend_exports.py
- [ ] DTA stable report/export outputs appear under stable partition with modality-appropriate summary text
</verification>

<success_criteria>
- All tasks completed
- DTA pass/warn/fail validation and method-context checks are productionized
- Stable DTA serialization status is aligned with Phase 2 stabilization intent
- Report/export outputs include DTA in stable artifacts with automated coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-dta-stabilization/02-02-SUMMARY.md`
</output>

